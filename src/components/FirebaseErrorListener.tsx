// src/components/FirebaseErrorListener.tsx
'use client';

/**
 * Placeholder that replaced the old Firebase error listener.
 * Keeps imports that reference it working while Firebase is removed.
 */
export default function FirebaseErrorListener() {
  return null;
}
